{
  "name": "demoji",
  "version": "0.1.0",
  "description": "CLI for scanning and sanitizing emoji usage in source files.",
  "type": "module",
  "bin": {
    "demoji": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test --experimental-strip-types tests/*.test.ts",
    "test:coverage": "npm run build && mkdir -p coverage && node --test --experimental-strip-types --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=coverage/lcov.info tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && npm run test",
    "security:audit": "npm audit --omit=dev --audit-level=high",
    "start": "node dist/index.js",
    "clean": "rm -rf dist coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/firblab-blog/demoji.git"
  },
  "keywords": [
    "emoji",
    "cli",
    "sanitizer"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
